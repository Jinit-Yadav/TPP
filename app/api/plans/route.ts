import { NextResponse } from 'next/server'
import { getCurrentUser } from '@/lib/auth'
import { connectMongo } from '@/lib/mongodb'
export async function GET() { const user = await getCurrentUser(); if (!user) return NextResponse.json({ error: 'Sign in required' }, { status: 401 }); const db = await connectMongo(); const plans = await db.collection('study_plans').find({ userId: user._id }).sort({ updatedAt: -1 }).toArray(); return NextResponse.json({ plans: plans.map((plan) => ({ id: String(plan._id), title: plan.title, planData: plan.planData, updatedAt: plan.updatedAt })) }) }
export async function POST(request: Request) { const user = await getCurrentUser(); if (!user) return NextResponse.json({ error: 'Sign in required' }, { status: 401 }); const body = await request.json().catch(() => null); if (!body?.planData) return NextResponse.json({ error: 'Plan data required' }, { status: 400 }); const db = await connectMongo(); const result = await db.collection('study_plans').insertOne({ userId: user._id, title: String(body.title || 'My study plan'), planData: body.planData, updatedAt: new Date() }); return NextResponse.json({ ok: true, id: String(result.insertedId) }) }
